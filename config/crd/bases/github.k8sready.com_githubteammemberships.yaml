---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: githubteammemberships.github.k8sready.com
spec:
  group: github.k8sready.com
  names:
    kind: GitHubTeamMembership
    listKind: GitHubTeamMembershipList
    plural: githubteammemberships
    shortNames:
    - ghteammember
    singular: githubteammembership
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.teamSlug
      name: Team
      type: string
    - jsonPath: .spec.username
      name: Username
      type: string
    - jsonPath: .status.role
      name: Role
      type: string
    - jsonPath: .status.invitationPending
      name: Pending
      type: boolean
    - jsonPath: .spec.deletionPolicy
      name: Deletion Policy
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GitHubTeamMembership manages one user's membership in a GitHub
          team.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GitHubTeamMembershipSpec assigns a user to a managed GitHub
              team.
            properties:
              deletionPolicy:
                default: Orphan
                description: DeletionPolicy controls whether deleting this resource
                  removes team membership.
                enum:
                - Orphan
                - Revoke
                type: string
              role:
                description: Role is the user's role in the team.
                enum:
                - member
                - maintainer
                type: string
              teamRef:
                description: TeamRef references a GitHubTeam in the same namespace.
                properties:
                  name:
                    description: Name is the Kubernetes GitHubTeam resource name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              username:
                description: Username is the GitHub login assigned to the team.
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: username is immutable
                  rule: self == oldSelf
            required:
            - role
            - teamRef
            - username
            type: object
          status:
            description: GitHubTeamMembershipStatus defines the observed team membership.
            properties:
              conditions:
                description: Conditions describe the current reconciliation state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              invitationPending:
                description: InvitationPending indicates that the user has not completed
                  organization membership.
                type: boolean
              observedGeneration:
                description: ObservedGeneration is the generation most recently reconciled.
                format: int64
                type: integer
              organization:
                description: Organization is the resolved GitHub organization.
                type: string
              providerConfigRef:
                description: ProviderConfigRef is the provider used during reconciliation.
                type: string
              role:
                description: Role is the observed team role.
                enum:
                - member
                - maintainer
                type: string
              state:
                description: State is the GitHub membership state, normally active
                  or pending.
                type: string
              team:
                description: Team is the referenced Kubernetes GitHubTeam name.
                type: string
              teamSlug:
                description: TeamSlug is the reconciled GitHub team slug.
                type: string
              username:
                description: Username is the reconciled GitHub login.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
