---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: githubrepositories.github.k8sready.com
spec:
  group: github.k8sready.com
  names:
    kind: GitHubRepository
    listKind: GitHubRepositoryList
    plural: githubrepositories
    shortNames:
    - ghrepo
    singular: githubrepository
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.providerConfigRef
      name: Provider
      type: string
    - jsonPath: .status.organization
      name: Organization
      type: string
    - jsonPath: .spec.name
      name: Repository
      type: string
    - jsonPath: .status.visibility
      name: Visibility
      type: string
    - jsonPath: .spec.deletionPolicy
      name: Deletion Policy
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GitHubRepository is the Schema for the githubrepositories API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GitHubRepositorySpec defines the desired state of GitHubRepository.
            properties:
              deletionPolicy:
                default: Orphan
                description: |-
                  DeletionPolicy determines whether deleting the custom resource also deletes
                  the remote GitHub repository.
                enum:
                - Orphan
                - Delete
                type: string
              name:
                description: Name is the name of the GitHub repository.
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              organization:
                description: |-
                  Organization is deprecated and ignored. Configure the organization through
                  GitHubProviderConfig instead. It is temporarily retained for v1alpha1 migration.
                type: string
                x-kubernetes-validations:
                - message: organization is immutable
                  rule: self == oldSelf
              providerConfigRef:
                default: default
                description: ProviderConfigRef references the cluster-scoped GitHubProviderConfig.
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: providerConfigRef is immutable
                  rule: self == oldSelf
              visibility:
                description: |-
                  Visibility determines whether the operator manages the repository visibility.
                  When omitted, new repositories are created as private, while existing repositories
                  keep their current visibility.
                enum:
                - public
                - private
                type: string
            required:
            - name
            type: object
          status:
            description: GitHubRepositoryStatus defines the observed state of GitHubRepository.
            properties:
              conditions:
                description: Conditions describe the current reconciliation state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the GitHubRepository generation
                  most recently reconciled.
                format: int64
                type: integer
              organization:
                description: Organization is the GitHub organization last observed
                  from the provider configuration.
                type: string
              providerConfigRef:
                description: ProviderConfigRef is the provider configuration used
                  during reconciliation.
                type: string
              repositoryId:
                description: RepositoryID is the numeric identifier assigned by GitHub.
                format: int64
                type: integer
              url:
                description: URL is the HTML URL of the GitHub repository.
                type: string
              visibility:
                description: Visibility is the repository visibility last observed
                  in GitHub.
                enum:
                - public
                - private
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
